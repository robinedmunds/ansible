all:
  hosts:
  children:
    controllers:
      hosts:
        ansible.local:
          ansible_connection: local

    docker:
      hosts:
        docker.local:
          ansible_host: docker.local

    kubernetes:
      hosts:
        1.k8s.local:
          ansible_host: 1.k8s.local

    getlegit:
      hosts:
        0.getlegit.co.uk:
          ansible_host: 192.168.40.128
          ansible_user: rocky

  vars:
    ansible_user: ansible
    ansible_port: 22
    ansible_connection: ssh
    ansible_python_interpreter: /usr/bin/python3
