all:
  hosts:
    ansible.tersedigital.co.uk:
      ansible_host: ansible.tersedigital.co.uk
  vars:
    ansible_user: ansible
    ansible_port: 22
    ansible_connection: ssh
    primary_url: ansible.tersedigital.co.uk
    urls_to_forward:
      - www.ansible.tersedigital.co.uk
