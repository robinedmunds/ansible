all:
  hosts:
  children:
    getlegit:
      hosts:
        0.getlegit.co.uk:
          ansible_host: 0.getlegit.co.uk

  vars:
    ansible_user: root
    ansible_port: 22
    ansible_connection: ssh
    ansible_python_interpreter: /usr/bin/python3

